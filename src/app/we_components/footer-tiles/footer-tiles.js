angular
  .module('app')
  .component('footerTiles', {
    templateUrl: 'app/we_components/footer-tiles/footer-tiles.html',
    controller: footerTiles
  });

  /** @ngInject */

function footerTiles($scope) {
}
